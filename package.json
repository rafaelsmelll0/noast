{
  "name": "noast",
  "private": true,
  "version": "0.6.2",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "test": "node --test tests/",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:all": "node --test tests/ && cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
